import { PlayerTextPipe } from './player-text.pipe';

describe('PlayerTextPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerTextPipe();
    expect(pipe).toBeTruthy();
  });
});
